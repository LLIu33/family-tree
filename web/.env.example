# Dev (Vite proxy → API on :3000). Leave unset in Docker prod same-origin builds.
VITE_API_URL=http://localhost:3000

# Render static site: set to your API public URL, e.g.
# VITE_API_URL=https://rodnik-api.onrender.com
